# satsim

[Install uv](https://docs.astral.sh/uv/getting-started/installation/)

Download deps using:

```bash
uv sync
```

Check code using:

```bash
uv run ty check .
uv run ruff check .
```

Run using:

```bash
uv run satsim
```
